import { UserTagDirective } from './user-tag.directive';

describe('UserTagDirective', () => {
  it('should create an instance', () => {
    const directive = new UserTagDirective();
    expect(directive).toBeTruthy();
  });
});
