import { MentionTransformPipe } from './mention-transform.pipe';

describe('MentionTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new MentionTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
